extends layout

block content
  head  
   link(rel="stylesheet", href="../stylesheets/books.css")
  body.electrician 
   
    h1 Elektryk 
    form(method='post' action='/logout').logoutForm
     button(type='submit' name='logout').logout Wyloguj
    img(src="../images/p2.png")
    form(method="post")
     each item in data
      .container
       label
        input(type="checkbox" name="service" value=item._id)
        span=item.nameOfService+' '+item.price+'zł'
     #nav
       input(type="text" class="name"  name="name" placeholder="podaj imie")
       input(type="email" class="email" name="email" placeholder="podaj email") 
       input(type="text"  name="address" placeholder="podaj adres")
       input(type="datetime-local" name="date")
       textarea(name="information", cols="60", rows="10") 
     input(type="submit" value='Potwierdz').confirm
         
       

     
